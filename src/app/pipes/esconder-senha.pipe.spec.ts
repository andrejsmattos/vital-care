import { EsconderSenhaPipe } from './esconder-senha.pipe';

describe('EsconderSenhaPipe', () => {
  it('create an instance', () => {
    const pipe = new EsconderSenhaPipe();
    expect(pipe).toBeTruthy();
  });
});
